@* @model List<Food> *@

@{
    Layout = LayoutService.GetLayout();
}

<section>
    <div class="container">
        <h5 class="testimonial__subtitle mb-4">Cart</h5>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Product Title</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        
                    </tbody>
                </table>
                <div class="mt-4">
                  @*   <h6>Subtotal: $<span class="cart__subtotal">@(Model.Sum((x => x.FoodPrice * x.Qty)))</span></h6> *@
                    @* <p>Taxes and shipping will calculate at checkout</p> *@
                    <div class="cart__page-btn">
                        <a class="addTOCart__btn btn me-4" href="/Food/Index#Foods">
                            Continue Shopping
                            @* <NavLink *@
                            @*     href=""> *@
                            @*     Continue *@
                            @*     Shopping *@
                            @* </NavLink> *@
                        </a>
                       
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>